<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:textColorLink">#000000</item>
    </style>
    <style name="CustomEditText">
        <!-- Customize your theme here. -->
        <item name="android:paddingLeft">5dp</item>
    </style>
    <style name="MenuStyle" parent="android:Widget.PopupMenu">
        <item name="android:textColor">@color/colorLetra</item>
        <item name="android:itemBackground">@color/colorBotones</item>
    </style>
</resources>
